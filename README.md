How I tested the code on VSCode

0. Install Node.js, NPM, and TS compiler if don't have one
1. Initialize typescript with "ts --init" in terminal to get tsconfig.json
2. Install ts-node with "npm install -g ts-node" in terminal
3. run "ts-node RomanToInt.ts"
